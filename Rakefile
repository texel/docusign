# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/docusign.rb'

Hoe.new('docusign', Docusign::VERSION) do |p|
  # p.rubyforge_name = 'docusignx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
